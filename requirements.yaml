name: evaluation
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pillow
  - pyyaml
  - protobuf
  - absl-py
  - pytorch=1.13.1
  - torchvision=0.14.1
  - cpuonly
  - pip:
    - gdown
    - google-cloud-storage
    - tabulate
    - git+https://github.com/fra31/auto-attack
